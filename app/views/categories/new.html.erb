<section>
  <h1>Cria categoria</h1>
  <%= form_for @category do |f| %>

    <div>
      <%= f.label :name, 'Categoria' %>
      <%= f.text_field :name %>
    </div>

    <div>
      <%= f.submit('Criar Categoria') %>
    </div>

  <% end %>
</section>
