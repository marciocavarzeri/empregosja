<%= simple_form_for @company do |f| %>
  <%= f.input :name, label: 'Nome', placeholder: 'Nome da empresa' %>
  <%= f.input :location, label: 'Local', placeholder: 'São Paulo, Rio de Janeiro...' %>
  <%= f.input :mail, label: 'Email', placeholder: 'exemplo@exemplo.com.br' %>
  <%= f.input :phone, label: 'Telefone', placeholder: '(xx) xxxxx-xxxx' %>
  <%= link_to 'Voltar', root_path, class: 'btn btn-default' %>
  <%= f.button :submit, submit_text %>
<% end %>
