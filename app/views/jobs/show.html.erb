<div class="row">
  <div class="col-md-7">
    <dl>
      <dt><h1><%= @job.title %></h1></dt>
      <dd><%= @job.category.name %></dd>
      <dd><%= @job.company.name %></dd>
      <dd><%= @job.description %></dd>
      <dd><%= @job.location %></dd>
    </dl>
    <% if @job.featured? %>
      <strong><%= '****Destaque****' %></strong>
    <% end %>
  </div>
</div>
<br>
<div class="row">
  <%= link_to 'Voltar', root_path, class: "btn btn-default" %>
  <%= link_to 'Editar', edit_job_path, class: "btn btn-default" %>
</div>
